export const selectWaterIntake = (state) => state.water.water;

export const selectIsLoadingWater = (state) => state.water.isLoading;

export const selectIsErrorWater = (state) => state.water.isError;